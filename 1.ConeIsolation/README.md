# Cone Isolation Code

1- chose your processes in the cone.py
2- check the ranges you want to loop pver in the steering file.
3. run the scan:
```bash
python steeringFile.py > scan.log 2>&1
```
4. run the log2csv code:
   ```bash
   python scanlog2csv.py
   ```
5. plotting stage:
   ```
   python plotting.py
   ```
